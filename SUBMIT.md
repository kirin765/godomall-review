# 심사 제출 체크리스트

> 마감 **2026-09-15**. 이 날까지 심사 제출 못 하면 KILL (원장 게이트).
> 심사 소요 기간은 아직 미확인 — 답변 오면 원장 날짜를 그만큼 순연한다.

## 코드 쪽 — 완료

| 항목 | 상태 |
|---|---|
| OAuth 설치·인증 | ✅ `onnurimun` 몰로 완주 실증 |
| 인증코드 → 토큰 교환 (1분 내) | ✅ |
| **갱신 토큰으로 재발급** | ✅ `getValidToken`이 만료 5분 전 자동 |
| Redirect URI 콘솔·코드 일치 | ✅ |
| 상품 조회 | ✅ |
| 게시판 쓰기 | ✅ `requests` 배열 + `client_ip` |
| 위젯 스크립트 삽입 | ✅ 실제 상품 상세 렌더 확인 |
| 엑셀 파싱·마스킹 | ✅ |
| 과금 상태 조회 | ✅ `/appstore/payments` |
| **앱 삭제 시 토큰 정리 (웹훅)** | ✅ 삭제·만료 웹훅(90077/90078) 수신 시 토큰·구독·사용량 폐기 |

**카페24가 개발정보 화면에 띄운 반려 1위 사유 3종을 전부 대응했다.**

## 재심사 대응 (판매종료 1회 — 2026-08-09)

**반려 사유**: 설치된 앱을 삭제 후 재설치 시 재설치가 불가능. 앱 삭제 때 사용자 토큰을 지우지 않아서.

**대응 2가지.**

1. **앱 삭제 웹훅 수신** — 개발자센터 WebHook에 아래를 등록하고 검증코드를 환경변수로 넣는다(등록 절차는 아래 STEP 01).
   - 수신 이벤트: **90077 (앱 삭제)**, 90078 (앱 만료)도 함께
   - 수신 URL: `https://cafe24-review-gamma.vercel.app/api/webhook/app`
   - 검증: `X-API-Key` 헤더 = `CAFE24_WEBHOOK_KEY` 환경변수. 틀리면 403.
   - 동작: 이벤트 수신 시 해당 몰의 `cafe24_token`·`subscription`(빌링키)·`usage_counter` 전부 삭제. 재설치하면 새 토큰을 받는다.
2. **재설치 방어** — 웹훅이 유실돼도 재설치가 막히지 않게 `/api/auth/install`이 저장된 토큰이 실제 살아있는지 `/admin/scripttags` 프로브로 확인하고, 죽은 토큰이면 재동의 OAuth로 흘려보낸다.

## 보안 심사 필수 2종 — 완료 (2026-07-28)

| 항목 | 상태 |
|---|---|
| **hmac 무결성 검증** | ✅ 정렬된 원본 쿼리스트링을 시크릿 키로 HMAC-SHA256 → base64 대조 |
| **timestamp ±2시간** | ✅ 벗어나면 401. Replay Attack 방지 |
| 알고리즘 검증 | ✅ 공식 문서 샘플 벡터와 일치 확인 |
| 몰 인증 | ✅ 쿼리 `mall_id`를 믿지 않는다. 서명된 세션 쿠키에서만 몰을 얻는다 |

**같이 막은 구멍.** 그 전까지는 `/admin?mall_id=x` 주소만 알면 아무나 그 몰의 상품 목록을 읽고
리뷰를 써넣을 수 있었다. 심사 항목이 아니라 실제 취약점이었다.

**운영 상 결과**: `/admin`은 카페24 관리자에서 앱을 실행해야만 열린다. 직접 주소로 못 들어간다.

실측 16종 전부 통과(2026-07-28, 운영 URL 대상): 세션 없는 API 401, hmac 없음·위조·파라미터 변조 401,
3시간 지난 timestamp 401, 1시간 전 timestamp 통과, 정상 실행 시 세션 발급 후 상품 조회 200,
위조 세션 401, 구매자용 위젯 API는 공개 유지.

## 개발자센터에서 채울 것 — 사용자

### STEP 01 개발정보 관리 — WebHook 등록 (재심사 반려 대응)

1. 개발자 어드민 → 상품관리 > App 관리 > **개발정보 관리**
2. **WebHook > [등록]**
   - 수신 이벤트: **앱 삭제 (90077)** — 앱 만료(90078)도 함께 넣어도 된다
   - 수신 URL: `https://cafe24-review-gamma.vercel.app/api/webhook/app`
   - 저장 후 **[TEST]** 로 샘플 발송 → 로그 확인
3. 같은 화면의 **WebHook 검증코드**를 복사해 Vercel 환경변수 `CAFE24_WEBHOOK_KEY`에 넣는다
   (로컬은 `.env.local`). 코드는 요청의 `X-API-Key` 헤더로 들어온다.
4. 앱 삭제 → 재설치 시나리오로 재검증: 삭제 시 웹훅 로그에 90077이 찍히고, 재설치가 새 토큰으로 진행되는지 확인.

### STEP 02 판매스토어 및 언어
- 노출 언어: 한국어

### STEP 03 판매정보 관리
| 항목 | 넣을 값 |
|---|---|
| 앱 이름 | **리뷰이사** (가칭. `cafe24-review`는 개발용 이름) |
| 카테고리 | **리뷰** |
| 가격 | **무료** (2026-07-28 변경 — 아래 참조) |
| 한 줄 소개 | 스마트스토어 구매평을 자사몰로 옮깁니다. 엑셀 한 번이면 끝. |
| 상세 설명 | 아래 초안 참조 |

### ⚠️ 무료로 전환 (2026-07-28, 사용자 확정)

**빨리 통과시켜 검증하는 것이 우선**이라 무료로 제출한다. 근거 넷.
- 인앱구매는 **"카페24와 협의"**가 필요해 09-15 마감을 위협한다
- 인앱구매면 결제를 우리가 직접 받아야 하는데 결제 수단이 없다
- 이 베팅이 재는 것은 **설치 수**다. 설치가 0이면 유료를 볼 필요도 없다
- 결제 관련 반려 사유가 사라져 통과 가능성이 오른다

**코드도 맞췄다.** 카페24 규정이 *"무료를 선택할 경우 구매자가 별도 과금 없이 사용할 수 있어야 한다"*고 하므로
**20건 제한을 제거**했다(실측: 25건 업로드 → 25건 전부 기록). 사용량 집계는 유지한다.

**⚠️ 게이트 처리**: `kill = 유료 전환 <10곳`은 사전 확정이라 바꾸지 않는다.
무료로 가면 유료 계측 시작이 늦어지므로 **유료 전환일까지 게이트를 순연**한다(원장 순연 규칙).

### 상세 설명 — HTML

`store-detail.html` 파일 내용을 그대로 붙여넣는다. 에디터의 HTML 모드로 전환 후 붙여넣기.

### 상세 설명 초안(텍스트판)

> **흩어진 구매평, 자사몰로 모으세요**
>
> 스마트스토어에 쌓인 구매평이 자사몰에는 없어서 첫 구매 전환이 안 나오고 있다면,
> 판매자센터에서 받은 엑셀 파일 하나로 옮길 수 있습니다.
>
> **이렇게 씁니다**
> 1. 스마트스토어 판매자센터 → 리뷰 관리 → 구매평 엑셀 다운로드
> 2. 앱에서 상품을 고르고 엑셀을 올립니다
> 3. 상품 상세에 구매평이 바로 붙습니다
>
> **가볍게 만들었습니다**
> - 설치 즉시 동작합니다. 소스 수정이 필요 없습니다
> - 상세페이지를 느리게 만들지 않습니다
> - 작성자 이름은 자동으로 가려집니다
>
> **문의 주시면 빠르게 답합니다.**

## 심사 전에 정리할 것 — 완료 (2026-07-28)

- [x] 진단 라우트 제거 — `/api/mall`·`/api/health`·`/api/billing` 전부 404 확인
- [x] **앱 설치 시 위젯 자동 삽입** — 콜백에서 중복 확인 후 등록, 관리자 화면으로 리디렉트
- [x] **무료 20건 제한** — 25건 업로드 시 17건만 기록·8건 건너뜀, 소진 후 402
- [x] 개인정보처리방침 `/privacy` — 마스킹만 저장·실명 미수집 명시

### 실측으로 잡은 카페24 제약

| 제약 | 값 |
|---|---|
| 게시판 쓰기 배치 | **한 호출에 10건.** 초과 시 422 "Request limit per call : 10" |
| `client_ip` | `0.0.0.0` 거부, `127.0.0.1` 통과 |
| 요청 래핑 | `requests` 배열. `request` 단수는 422 |
| API 버전 | `2026-03-01` |

## 유료(인앱결제) 실측 — 통과 (2026-09-01, 승인 직후)

고도몰 심사 승인 후 테스트 몰에서 인앱 유료결제 흐름을 실측으로 검증했다.

| 항목 | 결과 |
|---|---|
| 승인 후 앱 설치 상태 | `EXPIRED`(결제대기, 만료 21:43:55) — `GET /app-installed/status`로 확인 |
| `PUT /app-installed/extend` 호출 | **성공(204 요건).** `paymentType=CHARGE`, `price=9900` |
| 연장 후 상태 | `EXPIRED` → **`ACTIVE`**, 만료 `2026-10-01`로 설정 |
| 구독 기록(`app_subscriptions`) | ✅ 기록 → `checkQuota`/`getEntitlement`가 paid(무제한)로 판정 |
| 관리 화면 배너 | 「리뷰이사 플러스 — 무제한 이용 중」 표시 확인 |

**함께 고친 버그 3건 (이 실측으로 잡음)**

1. **`paymentType` 값을 틀리게 보냄** — 코드가 `PAID`를 보냈지만 워크스페이스 스펙은 `TRIAL|CHARGE`.
   `CHARGE`가 인앱 유료결제다. `normalizePaymentType()`로 스펙값으로 맵핑(이전 `PAID` 호환).
2. **`app_tokens`/`app_entitlement`/`app_subscriptions` 테이블이 안 만들어지던 버그** —
   postgres.js가 prepared statement로 보내서 `;`로 이어진 **다중 CREATE 문장을 거부**해
   (`cannot insert multiple commands into a prepared statement`) 조용히 실패. 문장별로 분리해 수정.
   이 때문에 결제 웹훅이 필요로 하는 몰 토큰이 DB에 저장되지 않았다.
3. **launch의 토큰 저장은 `void saveToken()`(비동기 무시)로 방치** — serverless에서 응답 직후
   인스턴스가 회수되어 DB 기록이 유실됨. `await`로 대기하도록 수정.
4. **옮긴 리뷰 원장 테이블 이름 충돌** — 공유 DB에서 제네릭한 `imported_review`가 다른 프로젝트의
   동명 테이블과 충돌해 `column "import_key" does not exist` 발생. 고유 이름 `godo_review_imported`로 변경.

**검증 후 정리**: 테스트로 만든 `diag-*` 구독 기록·entitlement 캐시를 삭제해 무료(20건) 기준으로
복구했다. 임시 진단 라우트(`/api/diag/*`)는 제거했다. 워크스페이스 `ACTIVE`(2026-10-01)는
심사자가 앱을 실행할 수 있도록 유지했고, 앱의 paid 판정은 구독 기록이 없으므로 무료로 판정한다.

## 미확인

| # | 항목 |
|---|---|
| 1 | 샵바이 솔루션 검증 — 고도몰만 포팅/실측함 |
| 2 | 실제 결제 콜백 payload 매핑 — 워크스페이스가 결제 완료를 판매사로 보내는 콜백 형식. 현재는 extend(관리자/헤더) 경로로 동일 처리 가능 |
