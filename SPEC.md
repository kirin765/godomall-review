# 카페24 리뷰 앱 — 최소 앱 스펙

> **이건 제품이 아니라 계측기다.** 재는 것은 "카페24 앱스토어가 이 각도에 유입을 만드는가" 하나다.
> 원장: `~/projects/misc/brain` → `wiki/projects.md` 트랙 "카페24 리뷰 앱", 게이트 2026-12-15.
> 근거 실측: `brain/reports/cafe24-appstore-shelf-2026-07-28.md`

## 게이트 (개발 착수 전 확정, 수정 금지)

| | |
|---|---|
| 출시 마감 | **2026-09-15.** 넘기면 KILL |
| 게이트 | **2026-12-15** |
| kill | **유료 전환 < 10곳** |
| 분모 | 앱스토어 노출 3개월 — 게이트에서 설치 수를 함께 기록 |

## 누구에게 파는가

**외부몰(쿠팡·스마트스토어)에 리뷰가 쌓여 있고, 그걸 카페24 자사몰로 옮기고 싶은 소형·초기 셀러.**

이 선반에서 유일하게 실측된 세그먼트다. 넥젠리뷰가 2026-06 진입해 2개월에 리뷰 16건을 받았고,
같은 코호트(ID 34000+, 83개) 2등이 7건이라 이상치다.

**차별 축은 가격이 아니라 응대 속도와 가벼움.** 실측된 반복 불만 셋이 근거다.
- "화면이 너무 어지럽네요" (스냅리뷰 ★3.7)
- "불필요한 기능들이 많고 무거워서 상세페이지 업로드 속도가 늦은감이 있습니다"
- "문의했을때 답변과 수정이 느려서 힘든적이 많..."

## 가격

**월 9,900원.** 넥젠과 같은 실측 가격점. 카페24 수수료 20%를 빼면 실수령 7,920원.
가격 탄력은 게이트 통과 후에 올려서 잰다.

## 범위 — 넣는 것

| # | 기능 | 왜 최소인가 |
|---|---|---|
| 1 | 카페24 OAuth 설치·인증 | 없으면 앱이 아니다 |
| 2 | 카페24 상품 목록 조회 | 매핑 대상 |
| 3 | **구매평 엑셀 업로드 → 파싱** | 핵심 가치. 크롤링 폐기 (아래 참조) |
| 4 | 수동 매핑 (외부몰 상품 ↔ 카페24 상품) | 자동 매칭은 안 만든다 |
| 5 | **개인정보 마스킹** | 법적 요건. 작성자명 부분 마스킹 |
| 6 | 리뷰 위젯 1종 — 상품 상세에 노출 | 스크립트 삽입 API |
| 7 | 카페24 인앱구매 과금 | 유료 전환을 세야 게이트가 성립 |

## 범위 — 빼는 것 (의도적)

| 뺀 것 | 이유 |
|---|---|
| **외부몰 2곳 지원** | **스마트스토어 하나만.** 쿠팡은 게이트 후 |
| **자동 크롤링 수집** | **폐기.** 서버에서 429고 공식 API에 리뷰가 없다 |
| 자동 상품 매칭 | 수동 붙여넣기로 충분하다 |
| AI 리뷰 요약·분석 | 넥젠의 축이다. 우리 축은 가벼움 |
| 위젯 디자인 여러 종 | 1종 |
| **별점 필터링 (부정 리뷰 제외)** | **표시광고법 논점. 안 만든다** |
| 알림톡·리뷰 유도·적립금 | 리뷰 관리 솔루션의 영역. 우리는 이관만 |
| 정기 자동 이관 | 수동 실행 버튼 하나. 리글매일의 영역 |

## 스택

- Next.js (App Router) + Vercel — 원장의 Vercel 배포 루프를 그대로 쓴다
- DB: Postgres (Neon 또는 Supabase)
- 카페24 Admin API + Front SDK(스크립트 삽입)

## 스마트스토어 리뷰 수집 — API 실측 (2026-07-28)

**뚫렸다.** 공개 상품 페이지에서 내부 API를 그대로 쓸 수 있다.

```
POST https://smartstore.naver.com/n/v1/contents/reviews/query-pages
     (브랜드스토어는 brand.naver.com, 경로 동일)

headers: content-type: application/json
         accept: application/json
         referer: <상품 URL>          ← 필수

body: {
  "checkoutMerchantNo": 500032486,
  "originProductNo": 11962182562,
  "page": 1,
  "pageSize": 20,
  "reviewSearchSortType": "REVIEW_CREATE_DATE_DESC"   // 또는 REVIEW_RANKING
}
```

**응답에서 쓰는 필드**

| 필드 | 용도 |
|---|---|
| `reviewScore` | 별점 |
| `reviewContent` | 본문 |
| `createDate` | 작성일 |
| **`maskedWriterId`** | **작성자명. `"cher****"` 형태로 네이버가 이미 마스킹해서 준다** |
| `productOptionContent` | 옵션 (예: "옵션선택: 혼합 4팩") |
| `reviewAttaches` | 첨부 사진 |
| `productName` · `productUrl` · `originProductNo` | 매핑 확인용 |

> ### ⭐ 개인정보 마스킹 요건이 크게 줄었다
> **네이버가 `maskedWriterId`를 이미 마스킹된 형태로 준다.** 우리는 그 값을 그대로 저장·노출하면 되고,
> 원본 `writerId`·`writerMemberNo`는 저장하지 않는다. 스펙의 "마스킹" 항목은 **"마스킹 필드만 저장"**으로 좁힌다.

### 🔴 기술 리스크 — 서버에서는 리뷰 API가 아예 안 열린다 (2026-07-28 실측, 미해결)

**앞서 "ID 추출 단계만 429"라고 적은 것은 틀렸다. 리뷰 API 자체가 서버에서 막힌다.**
브라우저에서 통과했던 것은 브라우저 세션·지문 덕이었다.

| 시도 | 결과 |
|---|---|
| Vercel 서버에서 호출 | **429** |
| 로컬(집 IP)에서 호출 | **429** — IP 문제가 아니다 |
| User-Agent 추가 | 429 |
| UA + Origin + Accept-Language + sec-ch-ua | 429 |
| naver.com 선방문으로 쿠키 획득 후 | **429** |
| **CDP 브라우저(사용자 세션)** | **200** ✅ |

**즉 지금 방식으로는 수집이 서버에서 안 된다. 수집이 없으면 제품이 없다.**

#### 선택지 판정 완료 (2026-07-28)

| # | 방향 | 판정 |
|---|---|---|
| 1 | 헤드리스 브라우저 수집 | 가능하나 Vercel 밖 워커가 필요하다. 계측기 단계에 과하다 |
| 2 | 네이버 커머스 공식 API | **🔴 사망.** 리뷰 API가 존재하지 않는다 (아래) |
| 3 | **구매평 엑셀 업로드** | **✅ 채택** |
| 4 | 각도 변경 | 보류 |

**2번이 죽은 근거 셋.**
- 공식 문서 `apicenter.commerce.naver.com` API 그룹 전체가 **인증·N배송·문의·상품·정산·주문·커머스솔루션·판매자정보** 여덟 개다. 리뷰가 없다
- GitHub `commerce-api-naver` 디스커션 #1909 공식 답변(2024-08-30): *"리뷰와 관련된 API 제공에 대해서 가까운 시일 내 제공 계획이 없다"*
- 문서 전체에 "리뷰·구매평·후기"라는 단어가 안 나온다

### ✅ 채택 — 구매평 엑셀 업로드

**스마트스토어 판매자센터 → [리뷰 관리] → 구매평 엑셀 다운로드**가 실재한다.
고객이 그 파일을 받아 우리 앱에 올리면 파싱해서 카페24 게시판으로 넣는다.

**열등한 선택이 아니다.**
- **업계 1위 리글도 매핑 데이터를 손으로 받는다**(FAQ 실측). 그리고 이관 비용이 **200~300만원**이다
- 크롤링이 없으니 **429·차단 대응·법적 회색지대가 전부 사라진다**
- 고객 손은 **엑셀 다운로드 1회**뿐이다
- 우리 포지션이 "가벼움"이라 어긋나지 않는다

**⚠️ 미확인**: 구매평 엑셀의 실제 컬럼 구성. 헤더명으로 유연하게 매핑하고, 첫 고객 파일로 확정한다.

### 카페24 게시판 쓰기 — 실측 (2026-07-28)

```
POST /api/v2/admin/boards/{board_no}/articles
body: { "shop_no": 1, "requests": [ {writer, title, content, client_ip}, ... ] }
```

**함정 둘을 실측으로 잡았다.**
1. **`request`(단수)가 아니라 `requests`(배열)다.** 단수로 보내면 422 "An invalid request is entered."
   래핑을 아예 빼면 400 "Please enter the Requests parameter."가 떠서 그걸로 알아냈다
2. **`client_ip`에 `0.0.0.0`은 거부된다.** 422 "Invalid IP address. (parameter.client_ip[0])". `127.0.0.1`은 통과
**⚠️ 넥젠이 어떻게 자동 수집하는지는 모른다.** 헤드리스거나 다른 경로일 텐데 지금 우리가 못 따라갈 비용이다.

## 열린 항목

| # | 항목 | 상태 |
|---|---|---|
| 1 | 카페24 앱 `client_id`·`client_secret` | **✅ 확보 (2026-07-28). `.env`, git 제외 확인** |
| 2 | redirect URI | 배포 후 확정 |
| 3 | **카페24 앱 심사 소요 기간** | ❓ 약관에 없다. **09-15 마감에 직접 영향** |
| 4 | 테스트용 카페24 쇼핑몰 | ❓ 무료 개설 가능 여부 확인 필요 |
| 5 | 스마트스토어 리뷰 수집 | ✅ **엑셀 업로드로 확정.** 크롤링 폐기 |
| 6 | 구매평 엑셀 컬럼 구성 | ❓ 첫 파일로 확정 |

## 하지 않기로 한 것

**고객이 생기면 그때 슬롯을 배정하거나 죽인다.** 이 앱이 문의 응대·기능 요청·업데이트 주기를 갖기 시작하면
그건 계측기가 아니라 제품이고, 원장 규칙상 Pick 슬롯을 먹는다 (brain `decision-log` 2026-07-28).
