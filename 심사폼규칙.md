# 고도몰 앱스토어 폼 입력 규칙 (반복 실수 방지)

심사팀 및 폼 제약에서 확인된 입력 규칙. 폼(판매 정보·심사요청 모달 등)에 텍스트를 붙여넣기 전에 반드시 체크한다.

## 1. 금지 특수문자 — `& " ' < >` (심사팀 안내, 2026-08-28)

> "특수문자 & " ' < >는 입력 불가합니다."

- `&"` `'` `<` `>` — **아스키 특수문자는 폼 입력 불가**. 붙여넣기 전 제거하거나 아래 한국어 기호로 교체.
- **사용 가능한 기호**: `「」` `“”` `‘’` `·` `—` `…` `( )` `[ ]` `.` `,` `/` `:` `→` `①②③` 등 한글 문서에 쓰는 전각 기호.
- 특히 **따옴표는 반드시 전각으로**: `"..."` ❌ → `「...」` 또는 `“...”` ✅
- 예시 수정: `"리뷰이사 플러스 — 무제한 이용 중"` ❌ → `「리뷰이사 플러스 — 무제한 이용 중」` ✅

## 2. 자릿수 제한

| 항목 | 제한 |
|---|---|
| 심사 방법 (심사요청 모달) | 2000자 |
| 앱 이름 | 50자 |
| 주요 기능 타이틀 | 15자 |
| 주요 기능 상세 | 50자 |

## 3. 이미지 규격

| 항목 | 규격 |
|---|---|
| 대표 이미지 (권장) | 820x480 (2배: 1640x960으로 준비) |
| 주요 기능 이미지 | 와이드 사용 중 (1640x960) |

## 4. URL 규칙

- 앱 URI / redirect URI는 `https://`만 허용 (http 불가)
- redirect URI는 개발자센터 "redirect URI" 등록값과 정확히 일치해야 함 (후행 슬래시 포함)
- 웹훅 수신 URL은 1개

## 5. 프로세스 규칙

- 심사 승인된 판매앱의 판매 정보를 변경하면 **재심사 필수** (승인 후에만 반영)
- 테스트 몰에는 **승인된 버전만 실행**됨 — 신규 코드의 실기능 검증은 재심사 경유

## 6. 저장·배포 순서 (폼 붙여넣기 전)

1. `심사방법.md`의 모달 텍스트가 위 규칙(특히 금지문자·2000자)을 통과하는지 확인
2. 판매 정보(가격·이미지·이용절차)에도 금지문자 없는지 확인
3. 그 후 심사요청 제출